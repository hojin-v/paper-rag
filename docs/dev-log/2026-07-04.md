# 2026-07-04 작업 일지

## 수행 작업

| 단위 | 내용 | 커밋 |
| --- | --- | --- |
| 설계 | v1+v2 논의를 DESIGN.md로 통합, ADR 2건(pgvector 단일 저장소, 파싱 이중 트랙) | `docs: 통합 설계서 및 ADR 2건 작성` |
| 스캐폴드 | pyproject(경량 코어+optional extras), docker-compose(postgres·redis·ollama), Settings/DB 모듈 | `build: 프로젝트 스캐폴드` |
| DB | 0001_init.sql — 9개 테이블, VECTOR(1024), HNSW 3개, 마이그레이션 러너 | `feat: DB 스키마 마이그레이션` |
| 파이프라인 | STEP 1~8 골격 + CLI(dry-run/skip-llm) + 배치 리포트 자동 생성 | `feat: 수집 파이프라인 STEP 1~8 골격 구현` |
| 검색 | 정확 매칭→유사 제안→대표/연관 선정→6시트 엑셀, FastAPI 3 엔드포인트 | `feat: 검색 서비스 및 엑셀 출력 구현` |
| 문서 | 구축 가이드 01~05 (단계별 형식), guide 템플릿 | `docs:` 커밋 4건 |

## 검증 결과

- pytest **30건 전체 통과** (외부 서비스 없이 오프라인 실행), ruff 통과
- E2E 스모크: 샘플 PDF 생성 → `python -m paperrag.ingest ... --backend simple --skip-llm --dry-run`
  → 참고문헌 제외·단락 병합·키워드 추출·8단계 전부 성공, 배치 리포트 자동 생성 확인
- 미검증(환경 제약): Docker Desktop WSL 통합 미활성으로 compose 스택 실기동·pgvector 마이그레이션
  적용은 실행하지 못함 — 코드·SQL은 테스트로 정적 검증 완료

## 작업 방식 기록

- 기계적 코드 작성은 Codex CLI 3개 태스크로 위임 (스펙 파일 → `codex exec --full-auto`)
  - 태스크 A(스캐폴드+스키마), B(파이프라인, Codex 측 ~101k 토큰), C(검색+엑셀, ~155k 토큰)
- Claude는 설계·태스크 스펙·검증·문서·git 관리 담당. 커밋 13건, 전부 작업 단위 분리 + Conventional Commits

## 2차 세션 — 사용자 시나리오 실 DB 검증 + UI

- **pgserver 도입**: Docker·sudo 불가 환경에서 pip 설치형 PostgreSQL(pgvector 내장)로 실 DB 검증 경로 확보
- **통합 테스트 4건** (`tests/integration/`): 실제 PostgreSQL+pgvector에서
  ① 요구서 예시 질의 정확 매칭 → 대표+연관 논문 ② 유사어("예지보전") 제안 Top-3 → 선택 → 결과
  ③ 참고문헌 제외 확인 ④ API 호출 + 엑셀 6시트 재로드 검증 — **전부 통과 (총 38 passed)**
- **실 DB 검증에서 버그 2건 발견·수정**: 관계 후보 SQL의 vector GROUP BY, top_relation 단방향 조회
  → 페이크 테스트만으로는 못 잡는 버그였음 (실 DB 통합 테스트의 가치 확인)
- **Streamlit UI**: 질의 → 결과 카드/유사 키워드 선택 → 엑셀 다운로드, ApiClient 오프라인 테스트 4건
- Codex 병렬 위임 2건 (D: ~249k, E 토큰). Codex 샌드박스에서 유닉스 소켓 생성이 막혀
  통합 테스트는 skip됐고, 메인 셸에서 실행해 통과 확인 — 위임 시 검증은 반드시 로컬에서 재실행할 것
- 요구사항 추적표 작성: `docs/design/REQUIREMENTS-TRACE.md` (20개 요구 ↔ 구현 ↔ 검증 상태)

## 3차 세션 — 임베딩 서버·UI 실기동·docling 실측

- **임베딩 서버** (`src/paperrag/embed/`): hash↔BGE-M3 전환형, 실기동 후 HttpEmbeddingClient로
  1024차원 계약 검증. compose `embedder` 서비스 추가 (Codex 태스크 F)
- **데모 서버 + UI 실기동**: `scripts/demo_server.py`(인메모리 시드, DB·LLM 불필요) 작성 →
  정확 매칭/유사 제안→선택/엑셀 6시트 다운로드 전 플로우를 HTTP로 검증, Streamlit 기동 확인
- **docling 실측 + 어댑터 결함 수정** (Codex 태스크 G): 초기 어댑터가 통짜 텍스트 변환이라
  표·참고문헌 타입 유실 → `iterate_items()` 12블록 매핑으로 재작성. 괘선 표 추출 성공(0→1),
  참고문헌 제외 정상화. 비교 리포트: `docs/reports/benchmarks/2026-07-04-layout-backends.md`
- 테스트 57건(오프라인 52 + 통합 5) 전체 통과
- **차단 사항**: Ollama 바이너리 다운로드가 권한 정책으로 거부됨 — 실 LLM 품질 측정은
  사용자 액션(직접 설치 또는 Docker 통합) 대기

## 다음 할 일

1. [사용자] Ollama 확보(직접 설치 또는 Docker 통합) → 실 LLM 품질 측정
2. Docker WSL 통합 → compose 스택 실기동 (guide 02·03)
3. BGE-M3 실모델 다운로드 → st 인코더 검증·실 임베딩 재적재
4. 실제 논문 PDF 10편 Phase 0 벤치마크 (mAP/CER/TEDS/E2E F1)
5. PP-OCR 스캔 경로 어댑터
